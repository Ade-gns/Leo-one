/**
 * executor_win.h — Interface d'exécution de scripts sous Windows
 *
 * Permet d'exécuter un script via un interpréteur whitelisté
 * ("cmd", "powershell") dans un processus enfant isolé, avec timeout et
 * capture de stdout/stderr. Miroir de platform/linux/executor_linux.h.
 */
#ifndef LEO_EXECUTOR_WIN_H
#define LEO_EXECUTOR_WIN_H

#include "../../src/executor.h"

#endif /* LEO_EXECUTOR_WIN_H */
